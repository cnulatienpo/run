{
  "name": "run",
  "version": "1.0.0",
  "description": "",
  "main": "electron-main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "start:headless": "dbus-run-session -- xvfb-run --server-args='-screen 0 1280x720x24' -- npx electron --no-sandbox --disable-gpu .",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "electron": "^28.0.0"
  }
}
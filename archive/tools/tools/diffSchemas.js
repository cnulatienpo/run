/**
 * Placeholder utility for future schema diffing functionality.
 *
 * @param {object} baseSchema - The baseline schema definition.
 * @param {object} compareSchema - The schema to compare against the base.
 * @returns {{ added: string[], removed: string[], changed: string[] }}
 */
export function diffSchemas(baseSchema, compareSchema) {
  void baseSchema;
  void compareSchema;
  return {
    added: [],
    removed: [],
    changed: [],
  };
}
